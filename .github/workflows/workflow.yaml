name: Bygg og Deploy Rekrutteringstreff-bruker
on:
  push:
  workflow_dispatch: # Legger til mulighet for manuell kjøring

jobs:
  build-and-deploy:
    name: Next
    uses: navikt/toi-github-actions-workflows/.github/workflows/build-and-deploy-nextjs.yaml@v10
    with:
      a11y-command: 'pnpm a11y'
      playwright-command: 'pnpm test'
      playwright-github-report: true
      dev-vars-path: '.nais/dev-gcp.json'
      prod-vars-path: '.nais/prod-gcp.json'
      deploy-dev-branch: 'msw'
    secrets: inherit
    permissions:
      contents: write
      id-token: write
      security-events: write
      pages: write
